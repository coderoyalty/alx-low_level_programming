#include <stdio.h>

/**
 * main - Entry point
 * Return: Success (0)
 */
int main(void)
{
	int val;

	for (val = 48; val <= 57; val++)
	{
		putchar(val);
	}
	for (val = 97; val <= 102; val++)
	{
		putchar(val);
	}
	return (0);
}
